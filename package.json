{
  "name": "flutter-beautify-docs",
  "private": true,
  "type": "module",
  "description": "Documentation site for the flutter_beautify CLI (not published — the CLI itself is a pub.dev package, see pubspec.yaml).",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
